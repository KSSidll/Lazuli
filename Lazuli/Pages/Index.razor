@page "/"
@inject NavigationManager NavigationManager


@code {
    protected override void OnInitialized()
    {
        // TODO check if logged in, if yes, navigate to main page
        Login();
    }

    private void Login()
    {
        NavigationManager.NavigateTo("/login");
    }
}
